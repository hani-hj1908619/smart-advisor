import { Box } from '@chakra-ui/react'
import Timetable from '../common/Timetable'

export default function DashTimetable({ semData }) {
    return (
        <Box>
            <Timetable />
        </Box>
    )
}